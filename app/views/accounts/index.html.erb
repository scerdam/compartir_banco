<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  <span class="py-1 lg:text-3xl font-light box-decoration-clone bg-gradient-to-r from-blue-600 to-grey-500 text-white px-5">
    Compartir
    Banco 💰
  </span>
  <div class="flex justify-between items-center pt-5 pb-12">
    <h1 class="font-bold text-4xl">Accounts</h1>
    <%= link_to "New account", new_account_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>
  <div id="accounts" class="min-w-full">
    <%= render @accounts %>
  </div>
</div>
